import './rebrand.css';
import { AppShell } from './app/AppShell';

/**
 * The app shell, in the real router so it can be clicked through rather than
 * looked at. Implemented from `Relethe App.dc.html`.
 */
export default function AppPreviewPage() {
  return <AppShell />;
}
